Commands API
============

``xarp.commands`` defines the messages sent between the Python server and an XR
client. Most applications use :mod:`xarp.express` instead of constructing these
models directly.

.. toctree::
   :maxdepth: 1

   protocol
   info
   ui
   sensing
   entities
